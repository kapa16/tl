
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СнятьНеактивныхВодителейСЛинии(Команда)
	
	ТекстНСТР = НСтр("en='Removing inactive drivers from the line...';ru='Снятие неактивных водителей с линии...'");
	Состояние(ТекстНСТР);
	
	уатМобильноеПриложениеВодителяСервер.СнятьНеактивныхВодителейСЛинии();
	
	ТекстНСТР = НСтр("en='Operation is complete.';ru='Операция завершена.'");
	Состояние(ТекстНСТР);
	
	Элементы.Список.Обновить();
	
КонецПроцедуры

#КонецОбласти
