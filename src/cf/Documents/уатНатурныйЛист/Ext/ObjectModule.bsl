
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	уатОбщегоНазначенияСервер.Заполнить(ЭтотОбъект, ДанныеЗаполнения);
	
	Если ТипЗнч(ДанныеЗаполнения) = Тип("ДокументСсылка.уатЗаказГрузоотправителя") Тогда
		СтанцияОтправления = ДанныеЗаполнения.АдресОтправления;
		СтанцияНазначения = ДанныеЗаполнения.АдресНазначения;
		ДатаОтправления = ДанныеЗаполнения.ОтправлениеС;
		ДатаПрибытия = ДанныеЗаполнения.ДоставкаС;
		ДокументОснование = ДанныеЗаполнения.Ссылка;
		
		Для каждого ТекСтрока Из ДанныеЗаполнения.ГрузовойСостав Цикл
			НоваяСтрокаВагоны = Вагоны.Добавить();
			НоваяСтрокаВагоны.МассаГруза = ТекСтрока.ВесБрутто;
			НоваяСтрокаВагоны.СтанцияНазначенияВагона = ДанныеЗаполнения.АдресНазначения;
			НоваяСтрокаВагоны.КодГруза = ТекСтрока.ГрузовоеМесто.КодЕТСНГ;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ДатаОтправления > ДатаПрибытия Тогда
		Отказ = Истина;
		ТекстНСтр = НСтр("ru='Дата прибытия меньше даты отправления'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстНСтр);
	КонецЕсли;
	
КонецПроцедуры
