
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	уатЗащищенныеФункцииСервер.уатСправочникФормаЭлементаПриСозданииНаСервере(Отказ, СтандартнаяОбработка, Объект, ЭтотОбъект, ДопПараметрыОткрытие);
	Если Отказ тогда
		Возврат;
	КонецЕсли;
	УстановитьСвязиПараметровВыбора();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	уатЗащищенныеФункцииКлиент.уатСправочникФормаЭлементаПриОткрытии(Отказ, ДопПараметрыОткрытие);
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьВидимость();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВнешняяСистемаПриИзменении(Элемент)
	Если Объект.ВнешняяСистема = ПредопределенноеЗначение("Справочник.уатВнешниеСистемы.СОГАЗ") Тогда
		Элементы.АдресСервиса.ПодсказкаВвода = "https://gw.sogaz.ru";
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ВнешняяСистемаНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	Отбор = Новый Структура("ТипВнешнейСистемы", ПредопределенноеЗначение("Перечисление.уатТипыВнешнихСистем.СервисСтрахования"));
	ОткрытьФорму("Справочник.уатВнешниеСистемы.Форма.ФормаВыбора", Новый Структура("Отбор", Отбор), Элемент);
КонецПроцедуры

&НаКлиенте
Процедура УслугаНачалоВыбора(Элемент, ДанныеВыбора, ВыборДобавлением, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Отбор = Новый Структура("ВидНоменклатуры", ПредопределенноеЗначение("Справочник.ВидыНоменклатуры.Услуга"));
	ОткрытьФорму("Справочник.Номенклатура.Форма.ФормаВыбора", Новый Структура("Отбор", Отбор), Элемент);
	
КонецПроцедуры

#КонецОбласти


#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПроверитьПодключение(Команда)
	
	Если Объект.ВнешняяСистема = ПредопределенноеЗначение("Справочник.уатВнешниеСистемы.СОГАЗ") Тогда
		ПроверитьПодключениеСОГАЗ();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьАдресПоУмолчанию(Команда)
	Объект.АдресСервиса = СтрЗаменить(СтрЗаменить(Элементы.АдресСервиса.ПодсказкаВвода, "Например:", ""), " ", "");
КонецПроцедуры

&НаКлиенте
Процедура ПараметрыПроксиСервера(Команда)
	
	ПараметрыФормы = Неопределено;
	Если ОбщегоНазначенияКлиент.ИнформационнаяБазаФайловая() Тогда
		ПараметрыФормы = Новый Структура("НастройкаПроксиНаКлиенте", Истина);
	КонецЕсли;
	
	ПолучениеФайловИзИнтернетаКлиент.ОткрытьФормуПараметровПроксиСервера(ПараметрыФормы);

КонецПроцедуры

#КонецОбласти


#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ПроверитьПодключениеСОГАЗ()
	
	ОшибкаЗаполнения = Ложь;
	
	Если Не ЗначениеЗаполнено(Объект.Логин) Тогда
		ТекстОшибки = НСтр("ru = 'Необходимо ввести логин.'; en = 'You must enter your login.'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки,, "Объект.Логин",, ОшибкаЗаполнения);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Объект.Пароль) Тогда
		ТекстОшибки = НСтр("ru = 'Необходимо ввести пароль.'; en = 'You must enter a password.'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки,, "Объект.Пароль",, ОшибкаЗаполнения);
	КонецЕсли;
	
	Если ОшибкаЗаполнения Тогда 
		Возврат;
	КонецЕсли;
	
	Если Не ПодключениеКСОГАЗДоступно() Тогда 
		ТекстОшибки = НСтр("ru = 'Подключение недоступно. (Подробности см. в журнале регистрации).'; en = 'Connection unavailable. (See the log for details.)'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки);
		Возврат;
	КонецЕсли;
	
	ТекстПредупреждение = НСтр("en='Connection check is completed successfully.';ru='Проверка подключения успешно завершена.'");
	ПоказатьПредупреждение(, ТекстПредупреждение);

КонецПроцедуры

&НаСервере
Функция ПодключениеКСОГАЗДоступно()
	
	ТекстОшибки = "";
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");      
	
	Попытка
		ВнутреннийХешИнтеграции = уатЗащищенныеФункцииСервер.ЗаполнитьВнутреннийКлючИнтеграции(Объект.Ссылка, Заголовки);
	Исключение
	КонецПопытки;  
	
	Если Объект.ИспользоватьТестовыйКонтур Тогда
		ТелоPOST = СтрШаблон("grant_type=password&username=%1&password=%2&scope=adinsure&client_secret=EF69597F-86BA-42FE-8A73-E044075F3FAE&client_id=sogaz-external-api",
			Объект.Логин, Объект.Пароль);
	Иначе
		ТелоPOST = СтрШаблон("grant_type=password&username=%1&password=%2&scope=adinsure&client_secret=3F6E0053-EB99-4F8F-B8E0-5F698EE9A3F7&client_id=sogaz-external-api",
			Объект.Логин, Объект.Пароль);
	КонецЕсли;
	
	АдресСервиса = АдресСервисаСтрахования();
	ПолныйАдресРесурса = "https://" + АдресСервиса + "/cargo/login";
	ТекстОшибки = "";
	
	HTTPОтвет = уатЗащищенныеФункцииСервер.ВыполнитьHTTPЗапрос(ПолныйАдресРесурса,
			"Сервис страхования. СОГАЗ", ТекстОшибки,, ТелоPOST, Заголовки,, Ложь);
	
	Попытка
		уатЗащищенныеФункцииСервер.ОбновитьВнутреннийКлючИнтеграции(Объект.Ссылка, HTTPОтвет.Заголовки, ВнутреннийХешИнтеграции);
	Исключение
	КонецПопытки;
	КодСостояния = ?(HTTPОтвет = 1, 1, HTTPОтвет.КодСостояния);
	
	Если ЗначениеЗаполнено(ТекстОшибки) 
		ИЛИ HTTPОтвет = 1 Или КодСостояния <> 200 Тогда
		СтрокаТекстаОшибки = НСтр("ru = 'Ошибка при попытке авторизации'; en = 'Error while attempting authorization'");
		ЗаписьЖурналаРегистрации(
		"Сервис страхования. СОГАЗ",
		УровеньЖурналаРегистрации.Ошибка,
		Метаданные.ОбщиеМодули.уатЗащищенныеФункцииСервер_проф,
		,
		СтрокаТекстаОшибки
		);
		Возврат Ложь;
	Иначе
		Возврат Истина;
	КонецЕсли;
	
КонецФункции

&НаКлиенте
Процедура УстановитьВидимость()
	
	Элементы.ГруппаАдресСервиса.Видимость = Истина;
	Элементы.НомерГенеральногоДоговора.Видимость = Ложь;
	
	Если Объект.ВнешняяСистема = ПредопределенноеЗначение("Справочник.уатВнешниеСистемы.СОГАЗ") Тогда
		
		Элементы.ГруппаАдресСервиса.Видимость = Ложь;
		Элементы.АдресСервиса.ПодсказкаВвода  = НСтр("ru = 'Например: https://gw.sogaz.ru'; en = 'For example: https://gw.sogaz.ru'");
		Элементы.НомерГенеральногоДоговора.Видимость = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура УстановитьСвязиПараметровВыбора()
	
	НовыйМассив = Новый Массив();
	
	НоваяСвязь = Новый СвязьПараметраВыбора("Отбор.Организация", "Объект.Организация");
	НовыйМассив.Добавить(НоваяСвязь);
	
	НоваяСвязь = Новый СвязьПараметраВыбора("Отбор.Владелец", "Объект.Контрагент");
	НовыйМассив.Добавить(НоваяСвязь);
	
	Элементы.ДоговорКонтрагента.СвязиПараметровВыбора = Новый ФиксированныйМассив(НовыйМассив);

КонецПроцедуры

Функция АдресСервисаСтрахования()
	
	Если Объект.ИспользоватьТестовыйКонтур Тогда
		Адрес = "stage-gw.sogaz.ru";
	Иначе
		Если уатЗащищенныеФункцииСервер_проф.ИспользоватьПроксиСерверРарус() Тогда
			Адрес = уатОбщегоНазначенияПовтИсп.ПолучитьАдресПроксиСервера() + "/ops/service/0bf71c6e-f3ff-464c-8925-ee345059149a";
		Иначе
			Адрес = "gw.sogaz.ru";
		КонецЕсли;
	КонецЕсли;
	
	Возврат Адрес;
	
КонецФункции

#КонецОбласти

