#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Параметры.Свойство("МЛ",         МЛ);
	Параметры.Свойство("ТС",         ТС);
	Параметры.Свойство("ДатаСтарта", ДатаСтарта); 
	Параметры.Свойство("Прицеп1",    Прицеп1);
	Параметры.Свойство("Прицеп2",    Прицеп2);
	Параметры.Свойство("ВесНетто",   ВесНетто);
	Параметры.Свойство("ВесТары",    ВесТары);
	
	ДатаВыезда = МЛ.Дата;
	Пробег     = МЛ.ПробегПорожний + МЛ.ПробегСГрузом;
	
	ОбновитьНастройки();
	ОбновитьСреднийРасходГСМ();

КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УстановитьВидимостьИсточникаЗаполненияГСМ(ОстатокТопливаВыезд); 
	УстановитьВидимостьИсточникаЗаполненияГСМВозврат(ОстатокТопливаВозврат);
КонецПроцедуры

&НаКлиенте
Процедура ДатаСтартаПриИзменении(Элемент)
	УстановитьВидимостьИсточникаЗаполненияГСМ(ОстатокТопливаВыезд);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ИсточникЗаполненияГСМПриИзменении(Элемент)  
	УстановитьВидимостьИсточникаЗаполненияГСМ(); 
КонецПроцедуры  

&НаКлиенте
Процедура ИсточникЗаполненияГСМВозвратПриИзменении(Элемент)
	УстановитьВидимостьИсточникаЗаполненияГСМВозврат();
КонецПроцедуры

&НаКлиенте
Процедура ОстатокТопливаВыездНачалоВыбора(Элемент, ДанныеВыбора, ВыборДобавлением, СтандартнаяОбработка)
	Если ИсточникЗаполненияГСМ = "По данным учета"
		ИЛИ ИсточникЗаполненияГСМ = "По мониторингу" Тогда  
		СтандартнаяОбработка = Ложь;
		УстановитьВидимостьИсточникаЗаполненияГСМ(ОстатокТопливаВыезд); 
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаОК(Команда)
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "entryPass";
	НоваяСтрока.Значение    = ЕстьПропускВГорода;
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "lowAxleLoad";
	НоваяСтрока.Значение    = НагрузкаНаОсь;
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "avoidToll";
	НоваяСтрока.Значение    = ИзбегатьПлатныхДорог;

	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "useCarRouter";
	НоваяСтрока.Значение    = ЛегковоеТС;
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "startVol";
	НоваяСтрока.Значение    = ?(ИсточникЗаполненияГСМ = "По мониторингу MultiGo", -1, ОстатокТопливаВыезд);
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "litersEndPath";
	НоваяСтрока.Значение    = ОстатокТопливаВозврат;
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "options.arrDates";
	НоваяСтрока.Значение    = ФиксированноеВремяПрибытия;
	
	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "options.tankVol";
	НоваяСтрока.Значение    = ОбъемБака;

	НоваяСтрока = НастройкиMultiGo.Добавить();
	НоваяСтрока.ПараметрAPI = "options.avgVolPer100";
	НоваяСтрока.Значение    = СреднийРасход;

	Настройки = ПреобразоватьТаблицуВМассивДляПередачиНаКлиент();
	Закрыть(Новый Структура("ДатаСтарта, Настройки", ДатаСтарта, Настройки));
	
КонецПроцедуры

&НаКлиенте
Процедура ДекорацияПрофильОбменаНажатие(Элемент)
	Оповещение = Новый ОписаниеОповещения("ДекорацияПрофильОбменаНажатиеЗавершение", ЭтотОбъект);
	ОткрытьФорму("Справочник.уатТС.Форма.ФормаЭлемента",Новый Структура("Ключ, Режим", ТС, "MultiGo"),ЭтотОбъект,,,,Оповещение);
КонецПроцедуры

&НаКлиенте
Процедура НастройкиMultiGoПриИзменении(Элемент)
	ТекущиеДанные = Элементы.НастройкиMultiGo.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ТекущиеДанные.Значение = Неопределено Тогда
		 ТекущиеДанные.Значение = "";
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РасшифровкаРасходаГСМ(Команда) 
	Отказ = Ложь;
	СписокНормРасхода = Новый СписокЗначений; 
	РасчетРасходаГСМ(МЛ, Отказ, СписокНормРасхода);

	Для Каждого ТекСтрока Из СписокНормРасхода Цикл
		ТекСтрока.Значение = (ТекСтрока.Значение * 100) / Пробег;
	КонецЦикла;
		
	Если Не Отказ Тогда
		ПараметрыОткрытия = Новый Структура("ТС, ДатаВыезда, СписокНормРасхода, ЭтоТЖ", ТС, ДатаВыезда, СписокНормРасхода, Ложь);
		ОткрытьФорму("Документ.уатПутевойЛист.Форма.РасшифровкаНормыРасходаГСМ", ПараметрыОткрытия, ЭтотОбъект,,,,, РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ПреобразоватьТаблицуВМассивДляПередачиНаКлиент()
	Настройки = уатАРМ.ПреобразоватьТаблицуВМассивДляПередачиНаКлиент(НастройкиMultiGo.Выгрузить());
	Возврат Настройки;
	
КонецФункции

&НаКлиенте
Процедура ДекорацияПрофильОбменаНажатиеЗавершение(Результат, ДополнительныеПараметры) Экспорт 
	ОбновитьНастройки(); 
	УстановитьВидимостьИсточникаЗаполненияГСМ(ОстатокТопливаВыезд); 
	УстановитьВидимостьИсточникаЗаполненияГСМВозврат(ОстатокТопливаВозврат);
КонецПроцедуры

&НаСервере
Процедура РасчетРасходаГСМ(МЛ, Отказ, СписокНормРасхода)
	
	Документы.уатМаршрутныйЛист.РасчетРасходаГСМ(МЛ, Отказ, СписокНормРасхода);
	
КонецПроцедуры


&НаСервере
Процедура ОбновитьНастройки()
	
	ОсновноеТопливо      = ТС.Модель.ОсновноеТопливо;
	УчетнаяЗаписьMultiGo = ТС.УчетнаяЗаписьMultiGo;
	ТекНастройкиMultiGo  = ТС.НастройкиMultiGo;
	
	НастройкиMultiGo.Очистить();
	Для Каждого ТекНастройка Из ТекНастройкиMultiGo Цикл 
		Если ТекНастройка.ПараметрAPI = "entryPass" Тогда
			ЕстьПропускВГорода   = ТекНастройка.Значение; 
		ИначеЕсли ТекНастройка.ПараметрAPI = "lowAxleLoad" Тогда
			НагрузкаНаОсь        = ТекНастройка.Значение;
		ИначеЕсли ТекНастройка.ПараметрAPI = "avoidToll" Тогда
			ИзбегатьПлатныхДорог = ТекНастройка.Значение;
		ИначеЕсли ТекНастройка.ПараметрAPI = "useCarRouter" Тогда
			ЛегковоеТС = ТекНастройка.Значение;
		ИначеЕсли ТекНастройка.ПараметрAPI = "startVol" Тогда
			ОстатокТопливаВыезд = ТекНастройка.Значение;
		ИначеЕсли ТекНастройка.ПараметрAPI = "litersEndPath" Тогда
			ОстатокТопливаВозврат = ТекНастройка.Значение;
		ИначеЕсли ТекНастройка.ПараметрAPI = "options.arrDates" Тогда
			ФиксированноеВремяПрибытия = ТекНастройка.Значение; 
		ИначеЕсли ТекНастройка.ПараметрAPI = "options.tankVol" Тогда
			ОбъемБака = ТекНастройка.Значение;
		ИначеЕсли ТекНастройка.ПараметрAPI = "options.avgVolPer100" Тогда
			СреднийРасход = ТекНастройка.Значение;
		Иначе
			НоваяСтрока = НастройкиMultiGo.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ТекНастройка);
		КонецЕсли;
	КонецЦикла;
	
	Если НастройкиMultiGo.Количество() = 0 Тогда
		МакетДанных = Справочники.уатТС.ПолучитьМакет("НастройкиMultiGo");
		КолСтрок    = МакетДанных.ВысотаТаблицы;
		Для Сч = 1 По КолСтрок Цикл 
			ПараметрAPI = МакетДанных.Область(сч,2,сч,2).Текст; 
			Значение    = МакетДанных.Область(сч,3,сч,3).Текст;
            ТипЗначения = МакетДанных.Область(сч,4,сч,4).ТипЗначения;
			Если Тип(ТипЗначения) = Тип("Булево") Тогда
				Значение = ?(Значение = "Истина", Истина, Ложь);
			ИначеЕсли Тип(ТипЗначения) = Тип("Число") Тогда
				Попытка
					Значение = Число(Значение);
				Исключение
					Значение = 0;
				КонецПопытки;
			Иначе
				Значение = Значение;
			КонецЕсли; 
			
			Если ПараметрAPI = "entryPass" Тогда
				ЕстьПропускВГорода   = Значение; 
			ИначеЕсли ПараметрAPI = "lowAxleLoad" Тогда
				НагрузкаНаОсь = Значение;
			ИначеЕсли ПараметрAPI = "avoidToll" Тогда
				ИзбегатьПлатныхДорог = Значение;
			ИначеЕсли ПараметрAPI = "useCarRouter" Тогда
				ЛегковоеТС = Значение;
			ИначеЕсли ПараметрAPI = "startVol" Тогда
				ОстатокТопливаВыезд = Значение;
			ИначеЕсли ПараметрAPI = "litersEndPath" Тогда
				ОстатокТопливаВозврат = Значение;
			ИначеЕсли ПараметрAPI = "options.arrDates" Тогда
				ФиксированноеВремяПрибытия = Значение;
			ИначеЕсли ПараметрAPI = "options.tankVol" Тогда
				ОбъемБака     = Значение;
			ИначеЕсли ПараметрAPI = "options.avgVolPer100" Тогда
				СреднийРасход = Значение;
			Иначе
				НоваяСтрока = НастройкиMultiGo.Добавить();
				НоваяСтрока.Наименование = МакетДанных.Область(сч,1,сч,1).Текст;
				НоваяСтрока.ПараметрAPI  = ПараметрAPI;
				НоваяСтрока.Описание     = МакетДанных.Область(сч,5,сч,5).Текст;
				НоваяСтрока.Значение     = Значение;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	ИсточникЗаполненияГСМ = "Фиксированное значение";

	СтрокиГСМ = ТекНастройкиMultiGo.НайтиСтроки(Новый Структура("ПараметрAPI", "startVol"));
	Если СтрокиГСМ.Количество() <> 0 Тогда 
		Значение = СтрокиГСМ[0].Значение;
		Если Значение = 0 Тогда
			ИсточникЗаполненияГСМ = "По данным учета";
		ИначеЕсли Значение = -1 Тогда
			ИсточникЗаполненияГСМ = "По мониторингу MultiGo";
		ИначеЕсли Значение = 1 Тогда
			ИсточникЗаполненияГСМ = "По мониторингу";
		Иначе
			ИсточникЗаполненияГСМ = "Фиксированное значение";
		КонецЕсли;
	КонецЕсли; 
	
	ИсточникЗаполненияГСМВозврат = "Фиксированное значение";
	Если ОстатокТопливаВозврат = 0 Тогда
		ИсточникЗаполненияГСМВозврат = "Максимальный объем на финише";
	ИначеЕсли ОстатокТопливаВозврат = -1 Тогда
		ИсточникЗаполненияГСМВозврат = "Челночный рейс";
	ИначеЕсли ОстатокТопливаВозврат = 1 Тогда
		ИсточникЗаполненияГСМВозврат = "Минимальный объем";
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ОбъемБака) Тогда
		ОбъемБака = ТС.Модель.ОбъемБака + ТС.Модель.ОбъемДопБака;
	КонецЕсли;
	
	Элементы.ДекорацияПрофильОбмена.Заголовок = ТС;
КонецПроцедуры

&НаСервере
Процедура ОбновитьСреднийРасходГСМ()  
	
	Отказ = Ложь;
	СписокНормРасхода = Новый СписокЗначений; 
	РасчетРасходаГСМ(МЛ, Отказ, СписокНормРасхода);
	
	СреднийРасход = 0; 
	Если Пробег = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого ТекСтрока Из СписокНормРасхода Цикл
		СреднийРасход = СреднийРасход + ((ТекСтрока.Значение * 100) / Пробег);
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура УстановитьВидимостьИсточникаЗаполненияГСМ(ТекОстатокТопливаВыезд = 0) 
	Если ИсточникЗаполненияГСМ = "По данным учета" Тогда  
		Элементы.ОстатокТопливаВыезд.Видимость = Истина;
		Элементы.ОстатокТопливаВыезд.КартинкаКнопкиВыбора = БиблиотекаКартинок.Обновить;
		
		ОстатокТопливаВыезд = уатОбщегоНазначения.уатОстатокГСМнаТС(ДатаСтарта, ТС,
		ОсновноеТопливо);
	ИначеЕсли ИсточникЗаполненияГСМ = "По мониторингу MultiGo" Тогда  
		Элементы.ОстатокТопливаВыезд.Видимость = Ложь;
		ОстатокТопливаВыезд = 0;
		
	ИначеЕсли ИсточникЗаполненияГСМ = "По мониторингу" Тогда 
		Элементы.ОстатокТопливаВыезд.Видимость = Истина;
		мсвОбъектыКЗапросу = Новый Массив();
		мсвОбъектыКЗапросу.Добавить(ТС);
		
		СводныеДанные = уатМониторинг.ПолучитьСводныеДанныеМониторинга(ДатаСтарта - 86400,
		ДатаСтарта, мсвОбъектыКЗапросу); 
		Если СводныеДанные.Количество() > 0 Тогда
			СтруктураДанных = СводныеДанные.Получить(ТС); 
			Если СтруктураДанных <> Неопределено Тогда
				ОстатокТопливаВыезд = СтруктураДанных.КонечныйУровеньТоплива;
			КонецЕсли;
		Иначе
			ОстатокТопливаВыезд = 0;
		КонецЕсли;
	Иначе
		Элементы.ОстатокТопливаВыезд.Видимость            = Истина;
		Элементы.ОстатокТопливаВыезд.КартинкаКнопкиВыбора = Новый Картинка(); 
		
		ОстатокТопливаВыезд = ТекОстатокТопливаВыезд;
	КонецЕсли;
КонецПроцедуры 

&НаКлиенте
Процедура УстановитьВидимостьИсточникаЗаполненияГСМВозврат(ТекОстатокТопливаВозврат = 0) 
	Если ИсточникЗаполненияГСМВозврат = "Максимальный объем на финише" Тогда  
		Элементы.ОстатокТопливаВозврат.Видимость = Ложь;
		ОстатокТопливаВозврат = 0;
	ИначеЕсли ИсточникЗаполненияГСМВозврат = "Минимальный объем" Тогда  
		Элементы.ОстатокТопливаВозврат.Видимость = Ложь;
		ОстатокТопливаВозврат = 1; 
	ИначеЕсли ИсточникЗаполненияГСМВозврат = "Челночный рейс" Тогда  
		Элементы.ОстатокТопливаВозврат.Видимость = Ложь;
		ОстатокТопливаВозврат = -1;
	Иначе  
		Элементы.ОстатокТопливаВозврат.Видимость = Истина;
		ОстатокТопливаВозврат = ТекОстатокТопливаВозврат;
	КонецЕсли;
КонецПроцедуры 


#КонецОбласти
