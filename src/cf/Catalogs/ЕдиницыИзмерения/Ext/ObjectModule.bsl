
#Область ОбработчикиСобытий

// Обработчик события ПередЗаписью .
//
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если (НЕ Отказ) И Коэффициент = 0 Тогда
		ТекстНСТР = НСтр("en='For %1 in units of measurement %2 does not specify a ratio! It will be set to 1.';ru='Для %1 у единицы измерения %2 не задан коэффициент! Он будет установлен равным 1.'");
		ТекстНСТР = СтрШаблон(ТекстНСТР, СокрЛП(Владелец), СокрЛП(Ссылка));
		уатОбщегоНазначенияТиповые.СообщитьОбОшибке(ТекстНСТР);
		Коэффициент = 1;
	КонецЕсли;

КонецПроцедуры // ПередЗаписью()

#КонецОбласти
