
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Не ПравоДоступа("Редактирование", Метаданные.Справочники.уатТС) Тогда 
		Элементы.ФормаИзменитьВыделенные.Видимость = Ложь;
		Элементы.СписокКонтекстноеМенюИзменитьВыделенные.Видимость = Ложь;
		Элементы.ФормаОткрытьМастерИзмененияНаименования.Видимость = Ложь;
	КонецЕсли;
	
	уатЗащищенныеФункцииСервер.уатСправочникФормаСпискаПриСозданииНаСервере(Отказ, СтандартнаяОбработка, ДопПараметрыОткрытие);
	Если Отказ ИЛИ ДопПараметрыОткрытие.ОшибкаИнициализацииСервер Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ДниДоОкончанияДействияДокументовТС", 0);
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ДатаОкончания", ТекущаяДата());
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ДниДоОкончанияДействияТопливныхКарт", 0);
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ТекущаяДата", НачалоДня(ТекущаяДата()));
		ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ТекущаяДатаВремя", ТекущаяДата());
		Возврат;
	КонецЕсли;
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
	// СтандартныеПодсистемы.ВерсионированиеОбъектов
    ВерсионированиеОбъектов.ПриСозданииНаСервере(ЭтотОбъект);
    // Конец СтандартныеПодсистемы.ВерсионированиеОбъектов
	
	Список.ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, "__СОСТОЯНИЕ_НЕДЕЙСТВУЕТ_", НСтр("ru = 'не действует'; en = ""doesn't work"""));
	Если ПравоДоступа("Просмотр", Метаданные.Справочники.уатРегистрационныеДокументыПрисоединенныеФайлы) Тогда
		Список.ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, """__ВладелецФайлаВладелецДокументов__""",
			"уатРегистрационныеДокументыПрисоединенныеФайлы.ВладелецФайла.ВладелецДокументов");
	Иначе
		Список.ТекстЗапроса = СтрЗаменить(Список.ТекстЗапроса, "__ВладелецФайлаВладелецДокументов__", 
			"ЗНАЧЕНИЕ(Справочник.уатТС.ПустаяСсылка)");
	КонецЕсли;
	
	ТекПользователь = ПользователиКлиентСервер.АвторизованныйПользователь();
	мВремяДоОкончанияДействияДокументовТС = уатПраваИНастройки.уатПолучитьПраваИНастройкиПользователя(
		уатОбщегоНазначенияПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекПользователь, "ОсновнаяОрганизация"),
		ПредопределенноеЗначение("ПланВидовХарактеристик.уатПраваИНастройки.ДниДоОкончанияДействияДокументовТС"));
	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ДниДоОкончанияДействияДокументовТС", мВремяДоОкончанияДействияДокументовТС);
	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ДатаОкончания", ТекущаяДата());
	
	мВремяДоОкончанияДействияТопливныхКарт = уатПраваИНастройки.уатПолучитьПраваИНастройкиПользователя(
		уатОбщегоНазначенияПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекПользователь, "ОсновнаяОрганизация"),
		ПредопределенноеЗначение("ПланВидовХарактеристик.уатПраваИНастройки.ДниДоОкончанияДействияТопливныхКарт"));

	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ДниДоОкончанияДействияТопливныхКарт", мВремяДоОкончанияДействияТопливныхКарт);
	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ТекущаяДата", НачалоДня(ТекущаяДата()));
	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(Список, "ТекущаяДатаВремя", ТекущаяДата());

	// уатУправлениеАвтотранспортом.МодификацияКонфигурации
	уатМодификацияКонфигурацииПереопределяемый.ПриСозданииНаСервере(ЭтотОбъект, Отказ, СтандартнаяОбработка);
	// Конец уатУправлениеАвтотранспортом.МодификацияКонфигурации
	
	АвторизованВнешнийПользователь = Ложь;
	АвторизованныйКонтрагент = уатЗащищенныеФункцииСервер_проф.АвторизованныйКонтрагент(АвторизованВнешнийПользователь);
	Если АвторизованВнешнийПользователь Тогда
		Элементы.Список.Отображение = ОтображениеТаблицы.Список;
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ВладелецТС", АвторизованныйКонтрагент,,,Истина);
		
		Элементы.ФормаОтображатьСтатусы.Видимость = Ложь;
		Элементы.ФормаПоказатьДопИнформацию.Видимость = Ложь;
		Элементы.ФормаОткрытьМастерИзмененияНаименования.Видимость = Ложь;
		Элементы.ФормаИзменитьВыделенные.Видимость = Ложь;
		Элементы.ОтобразитьВыбывшие.Видимость = Ложь;
		
		// Скрытие стандартных команд, если они есть
		МассивИменКоманд = Новый Массив;
		МассивИменКоманд.Добавить("ФормаСоздатьГруппу");
		МассивИменКоманд.Добавить("ФормаРежимПросмотра");
		МассивИменКоманд.Добавить("ФормаПеренестиЭлемент");
		МассивИменКоманд.Добавить("ФормаУровеньВверх");
		МассивИменКоманд.Добавить("ФормаУровеньВниз");
		МассивИменКоманд.Добавить("СписокКонтекстноеМенюСоздатьГруппу");
		МассивИменКоманд.Добавить("СписокКонтекстноеМенюПеренестиЭлемент");
		МассивИменКоманд.Добавить("СписокКонтекстноеМенюУровеньВверх");
		МассивИменКоманд.Добавить("СписокКонтекстноеМенюУровеньВниз");
		СкрытьКомандыФормыПриСуществовании(МассивИменКоманд);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	уатЗащищенныеФункцииКлиент.уатСправочникФормаСпискаПриОткрытии(Отказ, ДопПараметрыОткрытие);
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	СтруктураНастроек = ВосстановитьНастройки();
	НастройкиНекорректны = (СтруктураНастроек = Неопределено ИЛИ ТипЗнч(СтруктураНастроек) <> Тип("Структура"));
	
	// отображение выбывших ТС
	Если НастройкиНекорректны ИЛИ (НЕ СтруктураНастроек.Свойство("ОтображатьВыбывшие")) Тогда
		ОтображатьВыбывшие = Ложь;
	Иначе
		ОтображатьВыбывшие = СтруктураНастроек.ОтображатьВыбывшие;
	КонецЕсли;
	УстановитьОтборВыбывшие();
	
	// отображение строк с просроченными документами
	Если НастройкиНекорректны ИЛИ (НЕ СтруктураНастроек.Свойство("ЕстьПросроченныеДокументы")) Тогда
		ЕстьПросроченныеДокументы = Ложь;
	Иначе
		ЕстьПросроченныеДокументы = СтруктураНастроек.ЕстьПросроченныеДокументы;
	КонецЕсли;
	УстановитьОтборПросроченныеДокументы();
	
	// нажмем кнопку при открытии, если после закрытия она осталась нажатой
	Если НастройкиНекорректны ИЛИ (НЕ СтруктураНастроек.Свойство("ОтображатьСтатусы")) Тогда
		ОтображатьСтатусы = Ложь;
	Иначе
		ОтображатьСтатусы = СтруктураНастроек.ОтображатьСтатусы;
	КонецЕсли;
	Элементы.ФормаОтображатьСтатусы.Пометка = ОтображатьСтатусы;
	УстановитьВидимостьСтатусов();
	
	// отображение строки доп. информации
	Если НастройкиНекорректны ИЛИ (НЕ СтруктураНастроек.Свойство("ОтображатьДопИнфо")) Тогда
		ОтображатьДопИнфо = Ложь;
	Иначе
		ОтображатьДопИнфо = СтруктураНастроек.ОтображатьДопИнфо;
	КонецЕсли;
	Элементы.ФормаПоказатьДопИнформацию.Пометка = ОтображатьДопИнфо;
	ОтобразитьСкрытьДопИнформацию();
	
КонецПроцедуры

#КонецОбласти


#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ПанельДопИнформацииПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка)
	
	ТекущаяСтрока = Элементы.Список.ТекущаяСтрока;
	Если ТекущаяСтрока = Неопределено Тогда 
		Возврат;
	КонецЕсли;
	
	уатОбщегоНазначенияКлиент.ПанельДопИнформацииПоТСПриНажатии(
		ЭтотОбъект, 
		Элемент, 
		ДанныеСобытия, 
		СтандартнаяОбработка,
		ТекущаяСтрока
	);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтобразитьВыбывшиеПриИзменении(Элемент)
	УстановитьОтборВыбывшие();
	СохранитьНастройки();
КонецПроцедуры

&НаКлиенте
Процедура ЕстьПросроченныеДокументыОтборПриИзменении(Элемент)
	УстановитьОтборПросроченныеДокументы();
	СохранитьНастройки();
КонецПроцедуры

#КонецОбласти


#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Если Поле = Элементы.ПросроченныеДокументы И ВыбраннаяСтрока <> Неопределено Тогда
		СтандартнаяОбработка = Ложь;
		
		Отбор = Новый Структура("ВладелецДокументов, ПринадлежностьДокументов", ВыбраннаяСтрока,
			ПредопределенноеЗначение("Перечисление.уатПринадлежностьДокументов.ДокументыТС"));
		ПараметрыФормы = Новый Структура("Отбор", Отбор);
		
		ОткрытьФорму("Справочник.уатРегистрационныеДокументы.ФормаСписка", ПараметрыФормы, Элемент);
		
	ИначеЕсли Поле = Элементы.ТО И ВыбраннаяСтрока <> Неопределено Тогда
		СтандартнаяОбработка = Ложь;
		ПараметрыФормы = Новый Структура();
		ПараметрыФормы.Вставить("Отбор",                   Новый Структура("ТС", ВыбраннаяСтрока));
		ПараметрыФормы.Вставить("СформироватьПриОткрытии", Истина);
		ОткрытьФорму("Отчет.уатПриближениеСроковТО.Форма", ПараметрыФормы, ЭтотОбъект);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СписокПриАктивизацииСтроки(Элемент)
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПодключаемыеКомандыКлиент.НачатьОбновлениеКоманд(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
	ПодключитьОбработчикОжидания("Подключаемый_СписокПриАктивизацииСтроки", 0.2, Истина);
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СписокПриПолученииДанныхНаСервере(ИмяЭлемента, Настройки, Строки)
	КлючиСтрок = Строки.ПолучитьКлючи();
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ уатТСОбязательныеВидыДокументов.ВидДокумента) КАК КоличествоНеобходимых,
	|	уатТСОбязательныеВидыДокументов.Ссылка КАК Ссылка
	|ПОМЕСТИТЬ втКоличествоНеобходимых
	|ИЗ
	|	Справочник.уатТС.ОбязательныеВидыДокументов КАК уатТСОбязательныеВидыДокументов
	|ГДЕ
	|	уатТСОбязательныеВидыДокументов.Принадлежность = ЗНАЧЕНИЕ(Перечисление.уатПринадлежностьДокументов.ДокументыТС)
	|
	|СГРУППИРОВАТЬ ПО
	|	уатТСОбязательныеВидыДокументов.Ссылка
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	уатТСОбязательныеВидыДокументов.Ссылка КАК Ссылка,
	|	уатТСОбязательныеВидыДокументов.Принадлежность КАК Принадлежность,
	|	уатТСОбязательныеВидыДокументов.ВидДокумента КАК ВидДокумента,
	|	втКоличествоНеобходимых.КоличествоНеобходимых КАК КоличествоНеобходимых
	|ПОМЕСТИТЬ втНеобходимыеДоки
	|ИЗ
	|	Справочник.уатТС.ОбязательныеВидыДокументов КАК уатТСОбязательныеВидыДокументов
	|		ЛЕВОЕ СОЕДИНЕНИЕ втКоличествоНеобходимых КАК втКоличествоНеобходимых
	|		ПО уатТСОбязательныеВидыДокументов.Ссылка = втКоличествоНеобходимых.Ссылка
	|ГДЕ
	|	уатТСОбязательныеВидыДокументов.Принадлежность = ЗНАЧЕНИЕ(Перечисление.уатПринадлежностьДокументов.ДокументыТС)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	уатРегистрационныеДокументы.ВидДокумента КАК ВидДокумента,
	|	уатРегистрационныеДокументы.ВладелецДокументов КАК ВладелецДокументов,
	|	уатРегистрационныеДокументы.ПринадлежностьДокументов КАК ПринадлежностьДокументов
	|ПОМЕСТИТЬ втЗакрепленныеДоки
	|ИЗ
	|	Справочник.уатРегистрационныеДокументы КАК уатРегистрационныеДокументы
	|ГДЕ
	|	уатРегистрационныеДокументы.ПринадлежностьДокументов = ЗНАЧЕНИЕ(Перечисление.уатПринадлежностьДокументов.ДокументыТС)
	|	И НЕ уатРегистрационныеДокументы.ПометкаУдаления
	|
	|СГРУППИРОВАТЬ ПО
	|	уатРегистрационныеДокументы.ВладелецДокументов,
	|	уатРегистрационныеДокументы.ПринадлежностьДокументов,
	|	уатРегистрационныеДокументы.ВидДокумента
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	втНеобходимыеДоки.ВидДокумента КАК ВидДокумента,
	|	втНеобходимыеДоки.Ссылка КАК Ссылка,
	|	втЗакрепленныеДоки.ВидДокумента КАК ВидДокумента1,
	|	втЗакрепленныеДоки.ВладелецДокументов КАК ВладелецДокументов,
	|	ВЫБОР
	|		КОГДА втНеобходимыеДоки.ВидДокумента = втЗакрепленныеДоки.ВидДокумента
	|			ТОГДА 1
	|		ИНАЧЕ 0
	|	КОНЕЦ КАК Совпадения,
	|	втНеобходимыеДоки.КоличествоНеобходимых КАК КоличествоНеобходимых
	|ПОМЕСТИТЬ втПересечениеДоков
	|ИЗ
	|	втНеобходимыеДоки КАК втНеобходимыеДоки
	|		ЛЕВОЕ СОЕДИНЕНИЕ втЗакрепленныеДоки КАК втЗакрепленныеДоки
	|		ПО втНеобходимыеДоки.Ссылка = втЗакрепленныеДоки.ВладелецДокументов
	|			И втНеобходимыеДоки.ВидДокумента = втЗакрепленныеДоки.ВидДокумента
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	уатТС.Ссылка КАК ТС,
	|	СУММА(втПересечениеДоков.Совпадения) КАК Совпадения,
	|	втПересечениеДоков.КоличествоНеобходимых КАК КоличествоНеобходимых
	|ПОМЕСТИТЬ втСуммаСовпадений
	|ИЗ
	|	Справочник.уатТС КАК уатТС
	|		ЛЕВОЕ СОЕДИНЕНИЕ втПересечениеДоков КАК втПересечениеДоков
	|		ПО уатТС.Ссылка = втПересечениеДоков.Ссылка
	|ГДЕ
	|	уатТС.Ссылка В(&мсвТС)
	|
	|СГРУППИРОВАТЬ ПО
	|	уатТС.Ссылка,
	|	втПересечениеДоков.КоличествоНеобходимых
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	уатДокументыТС.ВладелецДокументов КАК ТС
	|ПОМЕСТИТЬ втПросроченныеДокументы
	|ИЗ
	|	Справочник.уатРегистрационныеДокументы КАК уатДокументыТС
	|ГДЕ
	|	уатДокументыТС.ПринадлежностьДокументов = ЗНАЧЕНИЕ(Перечисление.уатПринадлежностьДокументов.ДокументыТС)
	|	И уатДокументыТС.Статус <> ЗНАЧЕНИЕ(Перечисление.уатСтатусыдействия.НеДействует)
	|	И уатДокументыТС.ДатаОкончания <> ДАТАВРЕМЯ(1, 1, 1)
	|	И уатДокументыТС.ДатаОкончания <= ДОБАВИТЬКДАТЕ(&ТекущаяДата, ДЕНЬ, 1 * &ДниДоОкончанияДействияДокументовТС)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	уатПластиковыеКарты.КомуВыдана
	|ИЗ
	|	Справочник.уатПластиковыеКарты КАК уатПластиковыеКарты
	|ГДЕ
	|	уатПластиковыеКарты.Статус.Действует <> ЛОЖЬ
	|	И уатПластиковыеКарты.ДатаОкончания <> ДАТАВРЕМЯ(1, 1, 1)
	|	И уатПластиковыеКарты.ДатаОкончания <= ДОБАВИТЬКДАТЕ(&ТекущаяДата, ДЕНЬ, 1 * &ДниДоОкончанияДействияТопливныхКарт)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	уатТС.Ссылка КАК Ссылка,
	|	ВЫБОР
	|		КОГДА КОЛИЧЕСТВО(втПросроченныеДокументы.ТС) > 0
	|				ИЛИ втСуммаСовпадений.КоличествоНеобходимых > втСуммаСовпадений.Совпадения
	|			ТОГДА 0
	|		ИНАЧЕ 1
	|	КОНЕЦ КАК ЕстьПросроченныеДокументы
	|ИЗ
	|	Справочник.уатТС КАК уатТС
	|		ЛЕВОЕ СОЕДИНЕНИЕ втПросроченныеДокументы КАК втПросроченныеДокументы
	|		ПО уатТС.Ссылка = втПросроченныеДокументы.ТС
	|		ЛЕВОЕ СОЕДИНЕНИЕ втСуммаСовпадений КАК втСуммаСовпадений
	|		ПО уатТС.Ссылка = втСуммаСовпадений.ТС
	|ГДЕ
	|	уатТС.Ссылка В(&мсвТС)
	|
	|СГРУППИРОВАТЬ ПО
	|	уатТС.Ссылка,
	|	втСуммаСовпадений.КоличествоНеобходимых,
	|	втСуммаСовпадений.Совпадения";
	
	ТекПользователь = ПользователиКлиентСервер.АвторизованныйПользователь();
	мВремяДоОкончанияДействияДокументовТС = уатПраваИНастройки.уатПолучитьПраваИНастройкиПользователя(
		уатОбщегоНазначенияПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекПользователь, "ОсновнаяОрганизация"),
		ПредопределенноеЗначение("ПланВидовХарактеристик.уатПраваИНастройки.ДниДоОкончанияДействияДокументовТС"));
	мВремяДоОкончанияДействияТопливныхКарт = уатПраваИНастройки.уатПолучитьПраваИНастройкиПользователя(
		уатОбщегоНазначенияПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекПользователь, "ОсновнаяОрганизация"),
		ПредопределенноеЗначение("ПланВидовХарактеристик.уатПраваИНастройки.ДниДоОкончанияДействияТопливныхКарт"));
	Запрос.УстановитьПараметр("мсвТС", КлючиСтрок);
	Запрос.УстановитьПараметр("ДниДоОкончанияДействияДокументовТС", мВремяДоОкончанияДействияДокументовТС);
	Запрос.УстановитьПараметр("ДниДоОкончанияДействияТопливныхКарт", мВремяДоОкончанияДействияТопливныхКарт);
	Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДата());
	ВыборкаТС = Запрос.Выполнить().Выбрать();
	
	Пока ВыборкаТС.Следующий() Цикл
		СтрокаСписка        = Строки[ВыборкаТС.Ссылка];
		ДанныеСтрокаСписка  = СтрокаСписка.Данные;
		Если ДанныеСтрокаСписка.Свойство("ПросроченныеДокументы") Тогда
			ДанныеСтрокаСписка.ПросроченныеДокументы = ВыборкаТС.ЕстьПросроченныеДокументы;
		Иначе
			Возврат;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

#КонецОбласти


#Область ОбработчикиКомандФормы

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ВыполнитьКоманду(Команда)
	ПодключаемыеКомандыКлиент.НачатьВыполнениеКоманды(ЭтотОбъект, Команда, Элементы.Список);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

&НаКлиенте
Процедура ОтображатьСтатусы(Команда)
	Элементы.ФормаОтображатьСтатусы.Пометка = НЕ Элементы.ФормаОтображатьСтатусы.Пометка;
	УстановитьВидимостьСтатусов();
	СохранитьНастройки();
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьДопИнформацию(Команда)
	Элементы.ФормаПоказатьДопИнформацию.Пометка = НЕ Элементы.ФормаПоказатьДопИнформацию.Пометка;
	ОтобразитьСкрытьДопИнформацию();
	СохранитьНастройки();
КонецПроцедуры

&НаКлиенте
Процедура ИзменитьВыделенные(Команда)
	
	ГрупповоеИзменениеОбъектовКлиент.ИзменитьВыделенные(Элементы.Список, Список);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьМастерИзмененияНаименования(Команда)
	
	Оповещение = Новый ОписаниеОповещения("Подключаемый_ПослеГрупповогоИзмененияНаименования", ЭтотОбъект);
	ОткрытьФорму("Справочник.уатТС.Форма.МастерИзмененияНаименования",, ЭтотОбъект,,,, Оповещение);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ПродолжитьВыполнениеКомандыНаСервере(ПараметрыВыполнения, ДополнительныеПараметры) Экспорт
    ВыполнитьКомандуНаСервере(ПараметрыВыполнения);
КонецПроцедуры

&НаСервере
Процедура ВыполнитьКомандуНаСервере(ПараметрыВыполнения)
    ПодключаемыеКоманды.ВыполнитьКоманду(ЭтотОбъект, ПараметрыВыполнения, Элементы.Список);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКоманды()
    ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Элементы.Список);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

&НаСервереБезКонтекста
Функция ВосстановитьНастройки()
	СтруктураНастроек = ХранилищеНастроекДанныхФорм.Загрузить("Справочник.уатТС.Форма.ФормаСписка", "ОбщиеНастройки");
	
	Возврат СтруктураНастроек;
КонецФункции

&НаСервере
Процедура СохранитьНастройки()
	УстановитьПривилегированныйРежим(Истина);
	
	СтруктураНастроек = Новый Структура;
	СтруктураНастроек.Вставить("ОтображатьВыбывшие", ОтобразитьВыбывшие);
	СтруктураНастроек.Вставить("ОтображатьСтатусы" , Элементы.ФормаОтображатьСтатусы.Пометка);
	СтруктураНастроек.Вставить("ОтображатьДопИнфо" , Элементы.ФормаПоказатьДопИнформацию.Пометка);
	СтруктураНастроек.Вставить("ЕстьПросроченныеДокументы", ЕстьПросроченныеДокументы);
	
	ХранилищеНастроекДанныхФорм.Сохранить("Справочник.уатТС.Форма.ФормаСписка", "ОбщиеНастройки", СтруктураНастроек);
КонецПроцедуры

&НаКлиенте
Процедура УстановитьОтборВыбывшие()
	Если ОтобразитьВыбывшие Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ДатаВыбытия", '00010101',,, Ложь);
	Иначе
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ДатаВыбытия", '00010101',,, Истина);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура УстановитьОтборПросроченныеДокументы()
	ОтборыСписковКлиентСервер.ИзменитьЭлементОтбораСписка(Список, "ПросроченныеДокументы",
		?(ЕстьПросроченныеДокументы, 0, 1), ЕстьПросроченныеДокументы);
КонецПроцедуры
	
&НаКлиенте
Процедура УстановитьВидимостьСтатусов()
	Пометка = Элементы.ФормаОтображатьСтатусы.Пометка;
	Элементы.ТО.Видимость									 = Пометка;
	Элементы.ПросроченныеДокументы.Видимость				 = Пометка;
	Элементы.СтатусЗагрузкиДанныхИзМониторинга.Видимость	 = Пометка;
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_СписокПриАктивизацииСтроки()
	
	ОтобразитьСкрытьДопИнформацию();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтобразитьСкрытьДопИнформацию()
	
	флПоказатьДопИнфо = Элементы.ФормаПоказатьДопИнформацию.Пометка;
	Если флПоказатьДопИнфо И Не Элементы.ПанельДопИнформации.Видимость Тогда
		Элементы.ПанельДопИнформации.Видимость = Истина;
		
	ИначеЕсли Не флПоказатьДопИнфо И Элементы.ПанельДопИнформации.Видимость Тогда 
		Элементы.ПанельДопИнформации.Видимость = Ложь;
	КонецЕсли;
	
	Если флПоказатьДопИнфо Тогда
		Если Элементы.Список.ТекущаяСтрока = Неопределено Или ТипЗнч(Элементы.Список.ТекущаяСтрока) = Тип("СтрокаГруппировкиДинамическогоСписка") Тогда
			ПанельДопИнформации = "";
			Возврат;
		КонецЕсли;
		
		ПанельДопИнформации = уатОбщегоНазначения.СформироватьТекстДопИнформацииПоТС(Элементы.Список.ТекущаяСтрока);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПослеГрупповогоИзмененияНаименования(РезультатЗакрытия, ДополнительныеПараметры) Экспорт
	
	Элементы.Список.Обновить();
	
КонецПроцедуры

&НаСервере
Процедура СкрытьКомандыФормыПриСуществовании(МассивИменКоманд)
	Для Каждого ТекИмяКоманды Из МассивИменКоманд Цикл
		ТекущийЭлементФормы = Элементы.Найти(ТекИмяКоманды);
		Если ТекущийЭлементФормы <> Неопределено Тогда
			ТекущийЭлементФормы.Видимость = Ложь;
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

#КонецОбласти
