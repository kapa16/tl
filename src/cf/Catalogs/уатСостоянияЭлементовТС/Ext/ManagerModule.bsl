#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Выполняет заполнение предопределенных элементов справочника
//
Процедура ЗаполнитьПредопределенныеДанные() Экспорт
	
	СпрОбъект = Справочники.уатСостоянияЭлементовТС.Отсутствует.ПолучитьОбъект();
	СпрОбъект.ВидСостояния = Перечисления.уатСостоянияАгрегатов.Снято;
	СпрОбъект.Записать();
	
	СпрОбъект = Справочники.уатСостоянияЭлементовТС.Установлен.ПолучитьОбъект();
	СпрОбъект.ВидСостояния = Перечисления.уатСостоянияАгрегатов.УстановленоВРаботе;
	СпрОбъект.Записать();
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли