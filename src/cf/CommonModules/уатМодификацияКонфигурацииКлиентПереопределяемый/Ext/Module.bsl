////////////////////////////////////////////////////////////////////////////////
// Переопределяемые процедуры, вызываемые из обработчиков форм и реквизитов форм.
// 
////////////////////////////////////////////////////////////////////////////////

#Область СлужебныйПрограммныйИнтерфейс

#Область ЗаполнениеОбработчиковФормы

// Переопределяемая процедура, вызываемая из одноименного обработчика события формы.
//
// Параметры:
//  Форма - форма, из обработчика события которой происходит вызов процедуры.
//          см. справочную информацию по событиям управляемой формы.
//
Процедура ПриОткрытии(Форма, Отказ) Экспорт
		
КонецПроцедуры

// Переопределяемая процедура, вызываемая из одноименного обработчика события формы.
//
// Параметры:
//  Форма - форма, из обработчика события которой происходит вызов процедуры.
//          см. справочную информацию по событиям управляемой формы.
//
Процедура ПослеЗаписи(Форма, ПараметрыЗаписи) Экспорт
	
	// {УАТ_ПРОФ}
	уатМодификацияКонфигурацииКлиент_проф.ПослеЗаписи(Форма, ПараметрыЗаписи);
	// {/УАТ_ПРОФ}
	
КонецПроцедуры

#КонецОбласти

#Область ПоляФорм

// Переопределяемая процедура, вызываемая из обработчика события "ПриИзменении" поля формы.
// Вызов происходит из подключаемой процедуры "Подключаемый_ПолеПриИзменении".
//
// Параметры:
//  Форма   - форма, из обработчика события которой происходит вызов процедуры.
//            см. справочную информацию по событиям управляемой формы.
//  Элемент - элемент формы, вызвавший указанный обработчик.
//
Процедура ПолеФормыПриИзменении(Форма, Элемент) Экспорт
	
	// {УАТ_ПРОФ}
	уатМодификацияКонфигурацииКлиент_проф.ПолеФормыПриИзменении(Форма, Элемент);
	// {/УАТ_ПРОФ}
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ПолеФормыПриИзменении(Форма, Элемент);
	// {/УАТ_УЭ}
	
КонецПроцедуры

// Переопределяемая процедура, вызываемая из обработчика события "НачалоВыбора" поля формы.
// Вызов происходит из подключаемой процедуры "Подключаемый_ПолеНачалоВыбора".
//
// Параметры:
//  Форма                - форма, из обработчика события которой происходит вызов процедуры.
//                         см. справочную информацию по событиям управляемой формы.
//  Элемент              - элемент формы, вызвавший указанный обработчик.
//  ДанныеВыбора         - данные для выбора.
//  СтандартнаяОбработка - признак выполнения стандартной (системной) обработки события.
//
Процедура ПолеФормыНачалоВыбора(Форма, Элемент, ДанныеВыбора, СтандартнаяОбработка) Экспорт
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ПолеФормыНачалоВыбора(Форма, Элемент, ДанныеВыбора, СтандартнаяОбработка);
	// {/УАТ_УЭ}
	
КонецПроцедуры

// Переопределяемая процедура, вызываемая из обработчика события "ОбработкаВыбора" поля формы.
// Вызов происходит из подключаемой процедуры "Подключаемый_ПолеНачалоВыбора".
//
// Параметры:
//  Форма                - форма, из обработчика события которой происходит вызов процедуры.
//                         см. справочную информацию по событиям управляемой формы.
//  Элемент              - элемент формы, вызвавший указанный обработчик.
//  ВыбранноеЗначение    - выбранное значение, которое будет установлено как значения поля ввода.
//  СтандартнаяОбработка - признак выполнения стандартной (системной) обработки события.
//
Процедура ПолеФормыОбработкаВыбора(Форма, Элемент, ВыбранноеЗначение, СтандартнаяОбработка) Экспорт
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ПолеФормыОбработкаВыбора(Форма, Элемент, ВыбранноеЗначение, СтандартнаяОбработка);
	// {/УАТ_УЭ}
	
КонецПроцедуры

// Переопределяемая процедура, вызываемая из обработчика события "АвтоПодбор" поля формы.
//
Процедура ПолеФормыАвтоПодбор(Форма, Элемент, Текст, ДанныеВыбора, ПараметрыПолученияДанных, Ожидание, СтандартнаяОбработка) Экспорт
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ПолеФормыАвтоПодбор(Форма, Элемент, Текст, ДанныеВыбора, ПараметрыПолученияДанных, Ожидание, СтандартнаяОбработка);
	// {/УАТ_УЭ}
	
КонецПроцедуры

// Переопределяемая процедура, вызываемая из обработчика события "ОкончаниеВводаТекста" поля формы.
//
Процедура ПолеФормыОкончаниеВводаТекста(Форма, Элемент, Текст, ДанныеВыбора, ПараметрыПолученияДанных, СтандартнаяОбработка) Экспорт
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ПолеФормыОкончаниеВводаТекста(Форма, Элемент, Текст, ДанныеВыбора, ПараметрыПолученияДанных, СтандартнаяОбработка);
	// {/УАТ_УЭ}
	
КонецПроцедуры

// Переопределяемая процедура, вызываемая из обработчика события "Нажатие" поля формы.
// Вызов происходит из подключаемой процедуры "Подключаемый_ПолеНажатие".
//
// Параметры:
//  Форма                - форма, из обработчика события которой происходит вызов процедуры.
//                         см. справочную информацию по событиям управляемой формы.
//  Элемент              - элемент формы, вызвавший указанный обработчик.
//  СтандартнаяОбработка - признак выполнения стандартной (системной) обработки события.
//
Процедура ПолеФормыНажатие(Форма, Элемент, СтандартнаяОбработка) Экспорт
	
	// {УАТ_ПРОФ}
	уатМодификацияКонфигурацииКлиент_проф.ПолеФормыНажатие(Форма, Элемент, СтандартнаяОбработка);
	// {/УАТ_ПРОФ}
	
КонецПроцедуры

// Переопределяемая процедура, вызываемая из обработчика события "ПередУдалением" поля таблицы формы.
//
// Параметры:
//  Форма    - форма, из обработчика события которой происходит вызов процедуры.
//             см. справочную информацию по событиям управляемой формы.
//  Элемент  - элемент формы, вызвавший указанный обработчик.
//  Отказ    - признак отказа от удаления.
//
Процедура ПолеТаблицыФормыПередУдалением(Форма, Элемент, Отказ) Экспорт
	
	// {УАТ_ПРОФ}
	уатМодификацияКонфигурацииКлиент_проф.ПолеТаблицыФормыПередУдалением(Форма, Элемент, Отказ);
	// {/УАТ_ПРОФ}
	
КонецПроцедуры

#КонецОбласти

#Область Отчеты

// Обработчик расшифровки табличного документа формы отчета.
//
// Параметры:
//   ФормаОтчета - УправляемаяФорма - Форма отчета.
//   Элемент     - ПолеФормы        - Табличный документ.
//   Расшифровка          - Передается из параметров обработчика "как есть".
//   СтандартнаяОбработка - Передается из параметров обработчика "как есть".
//
// См. также:
//   "Расширение поля формы для поля табличного документа.ОбработкаРасшифровки" в синтакс-помощнике.
//
Процедура ОбработкаРасшифровки(ФормаОтчета, Элемент, Расшифровка, СтандартнаяОбработка) Экспорт
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ОбработкаРасшифровки(ФормаОтчета, Элемент, Расшифровка, СтандартнаяОбработка);
	// {/УАТ_УЭ}
	
КонецПроцедуры

#КонецОбласти

// Переопределяемая процедура, вызываемая из обработчика события "Подключаемый_ВыполнитьКоманду" команд формы.
//
// Параметры:
//  Форма   - форма, из обработчика события которой происходит вызов процедуры.
//            см. справочную информацию по событиям управляемой формы.
//  Команда - выполняемая команда.
//
Процедура ВыполнитьПодключаемуюКоманду(Форма, Команда) Экспорт
	
	// {УАТ_ПРОФ}
	уатМодификацияКонфигурацииКлиент_проф.ВыполнитьПодключаемуюКоманду(Форма, Команда);
	// {/УАТ_ПРОФ}
	
	// {УАТ_УЭ}
	уатМодификацияКонфигурацииКлиент_уэ.ВыполнитьПодключаемуюКоманду(Форма, Команда);
	// {/УАТ_УЭ}
	
	// УАТ СТД, ПРОФ, КОРП
	Если (Форма.ИмяФормы = "Обработка.слкМенеджерЛицензий.Форма.МенеджерЛицензийСЛК"
		Или Форма.ИмяФормы = "Обработка.слкМенеджерЛицензий.Форма.МенеджерЛицензийСЛКУпрощенный")
		И Команда.Имя = "_уатКомандаПерейтиНаЗащиту1СРарус" Тогда
		
		ФормаМенеджераЛицензийСЛКПерейтиНаЗащиту1СРарус();
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ФормаМенеджераЛицензийСЛКПерейтиНаЗащиту1СРарус()
	
	Оповещ = Новый ОписаниеОповещения("ПерейтиНаСистемуЗащиты1СРарусВопрос", уатМодификацияКонфигурацииКлиентПереопределяемый);
	ПоказатьВопрос(Оповещ,
		"После изменения системы защиты программа будет автоматически перезапущена.
		|Все несохраненные изменения будут потеряны.
		|Продолжить?",
		РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

Процедура ПерейтиНаСистемуЗащиты1СРарусВопрос(Результат, ДопПараметры) Экспорт
	Если Результат = КодВозвратаДиалога.Да Тогда
		уатЗащищенныеФункцииСервер_проф.УстановитьКонстантуВариантЗащиты1СРарус();
		ЗавершитьРаботуСистемы(Ложь, Истина);
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
