
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Если Параметры.Свойство("Заголовок") Тогда
		Заголовок = Параметры.Заголовок;
	КонецЕсли;
	
	Если Параметры.Свойство("Описание") Тогда
		Описание = Параметры.Описание;
		Элементы.Описание.Заголовок = Описание;
	КонецЕсли;
	
	Если Параметры.Свойство("ПредупреждениеWialon") 
		ИЛИ Параметры.Свойство("ПредупреждениеАРМЛогиста") Тогда
		Элементы.БольшеНеПоказывать.Видимость = Истина;
		Элементы.ФормаОтмена.Видимость        = Истина;
	Иначе 
		Элементы.БольшеНеПоказывать.Видимость = Ложь;
		Элементы.ФормаОтмена.Видимость        = Ложь;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ОК(Команда)
	
	Результат = Новый Структура();
	Результат.Вставить("Команда",            КодВозвратаДиалога.ОК);
	Результат.Вставить("БольшеНеПоказывать", БольшеНеПоказывать);
	
	Закрыть(Результат);
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	
	Закрыть();
	
КонецПроцедуры

#КонецОбласти
