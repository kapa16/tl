#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)
	
	уатЗащищенныеФункцииСервер.уатОтчетОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Настройки размещения в панели отчетов.
//
// Параметры:
//   Настройки – Коллекция – Используется для описания настроек отчетов и вариантов
//       см. описание к ВариантыОтчетов.ДеревоНастроекВариантовОтчетовКонфигурации().
//   НастройкиОтчета – СтрокаДереваЗначений – Настройки размещения всех вариантов отчета.
//       См. "Реквизиты для изменения" функции ВариантыОтчетов.ДеревоНастроекВариантовОтчетовКонфигурации().
//
// Описание:
//   См. ВариантыОтчетовПереопределяемый.НастроитьВариантыОтчетов().
//
// Вспомогательные методы:
//   НастройкиВарианта = ВариантыОтчетов.ОписаниеВарианта(Настройки, НастройкиОтчета, "<ИмяВарианта>");
//   ВариантыОтчетов.УстановитьРежимВыводаВПанеляхОтчетов(Настройки, НастройкиОтчета, Истина/Ложь); // Отчет
//   поддерживает только этот режим.
//
Процедура НастроитьВариантыОтчета(Настройки, НастройкиОтчета) Экспорт
	
	НастройкиВарианта = ВариантыОтчетов.ОписаниеВарианта(Настройки, НастройкиОтчета, "Анализ истории осмотров ТС по актам");
	НастройкиВарианта.Описание = НСтр("en='The report displays the history of vehicle inspections based on Acceptance"
		" and Transfer Certificates and other documents. The inspection documents are first detailed, then the units and elements of the vehicle.';"
		"ru='Отчет отображает историю осмотров ТС по Актам приема-передачи и другим документам. Детализируются сначала документы осмотра, потом агрегаты и элементы ТС.'");
	
	НастройкиВарианта = ВариантыОтчетов.ОписаниеВарианта(Настройки, НастройкиОтчета, "Анализ истории осмотров ТС по агрегатам и элементам");
	НастройкиВарианта.Описание = НСтр("en='The report displays the history of vehicle inspections based on Acceptance"
		" and Transfer Certificates and other documents. The units and elements of the vehicle are first detailed, then the inspection documents.';"
		"ru='Отчет отображает историю осмотров ТС по Актам приема-передачи и другим документам. Детализируются сначала агрегаты и элементы ТС, потом документы осмотра.'");
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли