#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)
	
	уатЗащищенныеФункцииСервер.уатОтчетОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Настройки размещения в панели отчетов.
//
// Параметры:
//   Настройки – Коллекция – Используется для описания настроек отчетов и вариантов
//       см. описание к ВариантыОтчетов.ДеревоНастроекВариантовОтчетовКонфигурации().
//   НастройкиОтчета – СтрокаДереваЗначений – Настройки размещения всех вариантов отчета.
//       См. "Реквизиты для изменения" функции ВариантыОтчетов.ДеревоНастроекВариантовОтчетовКонфигурации().
//
// Описание:
//   См. ВариантыОтчетовПереопределяемый.НастроитьВариантыОтчетов().
//
// Вспомогательные методы:
//   НастройкиВарианта = ВариантыОтчетов.ОписаниеВарианта(Настройки, НастройкиОтчета, "<ИмяВарианта>");
//   ВариантыОтчетов.УстановитьРежимВыводаВПанеляхОтчетов(Настройки, НастройкиОтчета, Истина/Ложь); // Отчет
//   поддерживает только этот режим.
//
Процедура НастроитьВариантыОтчета(Настройки, НастройкиОтчета) Экспорт
	
	НастройкиВарианта = ВариантыОтчетов.ОписаниеВарианта(Настройки, НастройкиОтчета, "Карточка работы ТС");
	НастройкиВарианта.Описание = НСтр("en='Report provides information about the operation of vehicles on waybills.';ru='Отчет позволяет получить информацию о работе транспортных средств по путевым листам.'");
	
КонецПроцедуры

// Используется для вычисляемого поля Экономия, округляет значение согласно настройке точности ГСМ
Функция Экономия(РасходПоНорме, РасходПоФакту, Точность) Экспорт
	Рез = РасходПоНорме - РасходПоФакту;
	Если Рез < 0 Тогда
		Рез = 0;
	КонецЕсли;
	Рез = Окр(Рез, Точность);
	Возврат Рез;
КонецФункции

// Используется для вычисляемого поля Пережог, округляет значение согласно настройке точности ГСМ
Функция Пережог(РасходПоНорме, РасходПоФакту, Точность) Экспорт
	Рез = РасходПоФакту - РасходПоНорме;
	Если Рез < 0 Тогда
		Рез = 0;
	КонецЕсли;
	Рез = Окр(Рез, Точность);
	Возврат Рез;
КонецФункции

#КонецОбласти

#КонецЕсли