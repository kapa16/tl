
#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыполнитьОбработку(Команда)
	
	Ошибки = ВыполнитьОбработкуСервер();
	ОбновитьИнтерфейс();
	//ПроверитьНаличиеОшибок(Ошибки.КоличествоОшибок, Ошибки.ПолныйТекстВсехОшибок);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ВыполнитьОбработкуСервер()
	
	Ошибки = Обработки.уатСтартовыйПомощникДляАнглийскойЛокализации.ВыполнитьДействияДляАнглийскойЛокализации();
	Возврат Ошибки;
	
КонецФункции

//&НаКлиенте
//Процедура ПроверитьНаличиеОшибок(КоличествоОшибок, ПолныйТекстВсехОшибок)
//	
//	Если КоличествоОшибок > 0 Тогда
//		ПараметрыПредупреждения = Новый Структура("Текст, Подробно");
//		Если КоличествоОшибок = 1 Тогда
//			ПараметрыПредупреждения.Текст = ПолныйТекстВсехОшибок;
//		Иначе
//			ПараметрыПредупреждения.Текст = СтрШаблон(НСтр("en='Performing failed (%1).';ru='При выполнении возникли ошибки (%1).'"), Строка(КоличествоОшибок));
//			ПараметрыПредупреждения.Подробно = ПолныйТекстВсехОшибок;
//		КонецЕсли;
//		СтандартныеПодсистемыКлиент.ВывестиПредупреждение(ЭтотОбъект, ПараметрыПредупреждения);
//	КонецЕсли;
//	
//КонецПроцедуры

#КонецОбласти